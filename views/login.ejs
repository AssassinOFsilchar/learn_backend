<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="styles.css" />
    <title>Document</title>
  </head>
  <body>
    <form action="/login" method="post">
      <h1>Login Now</h1>
      <div class="container">
        <input
          type="text"
          name="name"
          placeholder="Enter Name"
          value="<%=locals.name %>"
          required
        />
        <input
          type="email"
          name="email"
          placeholder="Enter Email ID"
          value="<%=locals.email %>"
          required
        />
        <input
          type="password"
          name="password"
          placeholder="Enter password"
          required
        />
        <p class="error"><%=locals.message%></p>
        <button class="button-29" type="submit">Login</button>
      </div>
    </form>
  </body>
</html>

<!-- button ka default submit hota hai form ke andar, shayad seh -->
